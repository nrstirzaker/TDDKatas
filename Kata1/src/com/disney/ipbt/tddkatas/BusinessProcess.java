package com.disney.ipbt.tddkatas;

public class BusinessProcess {

	/**
	 * This method must always return false unless
	 * 1) Equivalent values on each object are equal
	 * 		i.e. data1.startDateStatus = data2.startDateStatus & data1.endDateStatus = data2.endDateStatus
	 * 2) if one data object return a null value for the "start" or "end" then the other data object can 
	 * 		return 0 for the equivalent method 
	 * 
	 * see TruthTable.xlsx in the root of this project
	 * 
	 * 1) Create a test class first under test folder and in the package com.disney.ipbt.tddkatas
	 * 2) Write as many tests as you need need to validate that the method will work 
	 * 3) No complete the method below so that all you test pass
	 * 
	 */
	
	
	public Boolean isDataSimilar(Data data1, Data data2){
		return null;
	}
	
}
